/**
 * Firebase has been removed from this project.
 * Image storage and gallery management are now handled by Cloudinary.
 * Admin authentication is now handled by the api-server (JWT-based).
 *
 * See:
 *   src/lib/cloudinary.ts  — Cloudinary upload helper
 *   src/lib/api.ts         — API client for the backend
 */
export {};
